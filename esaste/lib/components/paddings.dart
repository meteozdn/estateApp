import 'package:flutter/material.dart';

class ProjectPaddings {
  static const mainPadding = EdgeInsets.symmetric(horizontal: 25, vertical: 10);
  static const miniMainPadding =
      EdgeInsets.symmetric(horizontal: 10, vertical: 10);
  static const horizontal = EdgeInsets.symmetric(horizontal: 25);
  static const onlyLeft = EdgeInsets.only(left: 25);
  static const onlyRight = EdgeInsets.only(right: 25);
  static const onlyMiniRight = EdgeInsets.only(right: 10);
  static const mainTop = EdgeInsets.only(left: 25, right: 25, top: 25);
  static const vertical = EdgeInsets.symmetric(vertical: 20);
  static const cardVertical = EdgeInsets.symmetric(vertical: 7);
  static const textButton = EdgeInsets.symmetric(horizontal: 30, vertical: 15);
  static const miniHorizontal = EdgeInsets.symmetric(horizontal: 5);
  static const onlyTop = EdgeInsets.symmetric(vertical: 25);
  static const cardmMinPadding = EdgeInsets.all(8);
}
